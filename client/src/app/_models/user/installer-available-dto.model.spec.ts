import { InstallerAvailableDto } from './installer-available-dto.model';

describe('InstallerAvailableDto', () => {
  it('should create an instance', () => {
    expect(new InstallerAvailableDto()).toBeTruthy();
  });
});
